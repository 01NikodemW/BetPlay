import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const BetsSectionContainer = styled(Box)(() => ({
  display: "flex",
  justifyContent: "center",
  marginTop: "124px",
}));

const DashboardStyles = () => null;
export default DashboardStyles;

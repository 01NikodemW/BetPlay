import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const FixtureDetailsBox = styled(Box)(() => ({
  position: "relative",
  display: "flex",
  flexDirection: "column",
}));

const FixtureDetailsStyles = () => null;
export default FixtureDetailsStyles;
